[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "axiom-vectorstore"
version = "0.1.0"
description = "Axiom RAG submodule"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/seiner69/axiom-vectorstore"
